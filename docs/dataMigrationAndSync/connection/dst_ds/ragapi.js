export const RagApi = {
    notice: [
        {
            key: 'Network Connectivity',
            desc: 'Ensure that the migration and sync node (Worker) can connect to the knowledge bases and LLMs.'
        }
    ],
    prepare: [
        {
            key: 'Port Preparation',
            desc: 'Allow the migration and sync node (Worker) to connect to the <b>LLM</b> and the <b>vector database</b>.'
        }
    ],
    params: [
        {
            key: 'uriPrefix',
            desc: 'The URI prefix of RagApi <b>chat service</b> to receive queries and send back the response generated by the model. Default value: <code>/v1/chat/completions</code>'
        },
        {
            key: 'contentUriPrefix',
            desc: 'The URI prefix of RagApi <b>information retrieval service</b> to retrive relevant vectors. Default value: <code>/v1/content/retrieve</code>'
        },
        {
            key: 'retrieveMaxResults',
            desc: 'Configure the maximum number of results returned by the retriever to limit the number of most relevant entries in the vector search'
        },
        {
            key: 'retrieveMinScore',
            desc: 'Configure the minimum score threshold for the retriever to return results. The higher the score is, the more relevant the result is. Only content with a score above this threshold will be considered.'
        },
        {
            key: 'contentPrompt',
            desc: 'Configure the prompt template, defining how to combine the user question with the retrieved content into a complete prompt. You can use <b>{{context}}</b> and <b>{{query}}</b> as variable placeholders.'
        },
        {
            key: 'enabledPromptFunctions',
            desc: 'Prompt functions that can be enabled (seperated by comma).<br /> Optional functions:<br /> ' +
                '\n - <b>KNOWLEDGE_SELECT</b>(Select the most relevant content automatically. Supports automatic routing of multiple knowledge bases.)<br /> ' +
                '\n - <b>QUERY_COMPRESS</b>(Compress queries)<br /> ' +
                '\n - <b>QUERY_EXTEND</b>（Extend queries）<br /> ' +
                '\n \n Example: <code>KNOWLEDGE_SELECT,QUERY_COMPRESS</code>'
        },
        {
            key: 'compressContentPrompt',
            desc: 'Prompt for query compression, telling the system how to simplify the input text. You can use <b>{{chatMemory}}</b> and <b>{{query}}</b> as variable placeholders. This will only work when <b>QUERY_COMPRESS</b> is enabled in <b>enabledPromptFunctions</b>.'
        },
        {
            key: 'extendContentPrompt',
            desc: 'Prompt for query extention, telling the system how to enrich the original query by adding relevant context. You can use {{query}} as a variable placeholder, which only works when <b>QUERY_EXTEND</b> is enabled in <b>enabledPromptFunctions</b>.'
        },
        {
            key: 'extendContentCount',
            desc: 'Set the number of entries generated when content is extended. This only works when <b>QUERY_EXTEND</b> is enabled in <b>enabledPromptFunctions</b>.'
        },
        {
            key: 'mcpServers',
            desc: 'JSON for configuring available MCP servers, defining how to call the tools (e.g. command line or HTTP).' +
                '<br> eg: {\n' +
                '    "mcpServers": {\n' +
                '        "github": {\n' +
                '            "command": "docker",\n' +
                '            "args": [\n' +
                '                "run",\n' +
                '                "-i",\n' +
                '                "--rm",\n' +
                '                "-e",\n' +
                '                "GITHUB_PERSONAL_ACCESS_TOKEN",\n' +
                '                "mcp/github"\n' +
                '            ],\n' +
                '            "env": {\n' +
                '                "GITHUB_PERSONAL_ACCESS_TOKEN": "<YOUR_TOKEN>"\n' +
                '            }\n' +
                '        }\n' +
                '    }\n' +
                '}'
        },
        {
            key: 'maxChatMemory',
            desc: 'The maximum number of messages retained in the chat context, defining how many rounds of conversation can be seen during model reasoning.'
        },
        {
            key: 'toolMaxInvokeCount',
            desc: 'The maximum number of tool calls allowed in a single session, used to limit the call depth and prevent tool execution from falling into an infinite loop.'
        },
    ],
    master_function: [
        {
            key: 'Knowledge Selection（KNOWLEDGE_SELECT）',
            desc: 'According to the user query, the most relevant knowledge is automatically filtered from the retrieval results to improve the accuracy of the generated answers.'
        },
        {
            key: 'Query Compression（QUERY_COMPRESS）',
            desc: 'Semantically compress the original query to remove redundancy and keep the core content, optimizing the retrieval performance.'
        },
        {
            key: 'Query Extension（QUERY_EXTEND）',
            desc: 'Automatically expand user query and introduce potentially relevant information or synonymous expressions to expand semantic coverage.'
        },
        {
            key: 'MCP Tool Call',
            desc: 'Call tool chains (such as GitHub queries, Shell commands, etc.) configured on the MCP platform to automatically call external systems to complete tasks or offer more information in Q&A.'
        },
    ]
}


